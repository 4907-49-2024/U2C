package unittest.xmiParser;

import com.sdmetrics.model.ModelElement;
import org.junit.jupiter.api.BeforeAll;
import org.junit.jupiter.api.Test;
import filters.xmiParser.XMIParser;
import filters.xmiParser.XMIParserConfig;

import java.util.*;

import static org.junit.jupiter.api.Assertions.assertEquals;

/**
 * Test that all diagrams have the elements we expect.
 */
class XMIParserTest {
    // At the moment, cannot fathom how to test the diagram files without processing it first...
    //  It makes more sense to test the steps down the line.
}